#include <stdio.h>

int main(){
    float temperatura = 31.5;
    int resultado;

    (temperatura >= 37.5) ? printf("Voce está com febre\n") : printf("Voce não está com febre\n");

// "?" (Se for) 
// ":" (Se não for)
   
    return 0; 
 
}

