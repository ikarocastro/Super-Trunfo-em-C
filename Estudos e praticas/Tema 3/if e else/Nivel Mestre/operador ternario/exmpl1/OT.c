#include <stdio.h>

int main(){
    int idade = 16;
    int resultado;

    (idade >= 18) ? printf("Voce é Maior de idade\n") : printf("Voce é Menor de idade\n");

    //  condição ? expressão1 (verdadeiro) : expressão2(falso);
   
    return 0;
}