#include <stdio.h>

int main() {

  int a = -10;

  if (!(a > 0)){ //(!(a > 0)
    printf("A variavel e negativa!\n");
        //printf("a não e um numero positivo\n");

  }else{
    printf("A variavel e positiva!");
  }

    //observe que por conta do NOT o que é apresentado no terminal e a parte do ELSE que e para ser falso mais a parter que e verdadeira e a do IF mais pela troca de valores dada pelo NOT não acontece!

    //Se eu colocar -10 no valor de A a condição if que e verdadeira aponta a falsa!

}